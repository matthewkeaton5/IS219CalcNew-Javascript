function Skewness(mean, std, sumOfAllData, numberOfDataPoints){
    //for skewness we need the standard deviation (std), mean of the
    //distribution, sum of all data points in the distribution, number of data points = amountOfDataPoints
//looking for a skewness formula
    return 0;
}

module.exports = Skewness;